[pytest]
; In development run "pytest test.py" to avoid "ERROR: file or directory not found: /app".
; Place student's code in toppings.json before running tests.
testpaths = /app
python_files = test.py
